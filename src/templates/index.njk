n<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>🍔 FoodWheels India | Home</title>

  <!-- Base URL for GitHub Pages -->
  <base href="https://manasa272.github.io/FoodWheels_project/">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    /* --- General Styles --- */
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #fff8f0;
      color: #333;
      margin: 0;
      scroll-behavior: smooth;
    }

    /* --- Navbar --- */
    .navbar {
      background-color: #ff7b00;
      position: sticky;
      top: 0;
      z-index: 9999;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.15);
    }

    .navbar-brand {
      font-weight: 700;
      color: white !important;
      font-size: 1.8rem;
    }

    .nav-link {
      color: white !important;
      font-weight: 600;
      font-size: 1.1rem;
      transition: color 0.3s ease;
    }

    .nav-link:hover {
      color: #ffd54f !important;
    }

    /* --- Hero Section with background slider --- */
    .hero {
      height: 90vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: white;
      position: relative;
      overflow: hidden;
      padding: 0 20px;
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 5;
      max-width: 700px;
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 800;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
      margin-bottom: 15px;
    }

    .hero p {
      font-size: 1.4rem;
      margin-bottom: 25px;
      text-shadow: 1px 1px 8px rgba(0, 0, 0, 0.5);
      line-height: 1.4;
    }

    .btn-order {
      background-color: #ff6f00;
      color: white;
      padding: 12px 30px;
      font-weight: 700;
      border: none;
      border-radius: 30px;
      transition: 0.3s ease;
      text-decoration: none;
      display: inline-block;
      box-shadow: 0 5px 15px rgba(255, 111, 0, 0.6);
      user-select: none;
    }

    .btn-order:hover {
      background-color: #e65100;
      transform: scale(1.1);
      box-shadow: 0 8px 20px rgba(230, 81, 0, 0.8);
    }

    /* Background images for slider */
    .hero-bg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-size: cover;
      background-position: center;
      opacity: 0;
      transition: opacity 1.5s ease-in-out;
      z-index: 1;
    }

    .hero-bg.active {
      opacity: 1;
      z-index: 2;
    }

    /* --- Why Choose Us --- */
    .why-section {
      padding: 60px 15px;
      text-align: center;
      background-color: #fff;
    }

    .why-section h2 {
      color: #ff6f00;
      font-weight: 800;
      margin-bottom: 40px;
      font-size: 2.5rem;
      letter-spacing: 1px;
    }

    .why-box {
      font-size: 1.2rem;
      font-weight: 600;
      padding: 25px;
      border-radius: 12px;
      background: #fff4e6;
      box-shadow: 0 4px 12px rgba(255, 123, 0, 0.15);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: default;
    }

    .why-box:hover {
      transform: translateY(-8px);
      box-shadow: 0 10px 25px rgba(255, 123, 0, 0.3);
    }

    /* --- Menu Preview --- */
    .menu-preview {
      padding: 70px 15px 90px;
      background: linear-gradient(135deg, #fff8f0, #fff3e0);
      text-align: center;
    }

    .menu-preview h2 {
      color: #ff6f00;
      font-weight: 800;
      margin-bottom: 50px;
      font-size: 2.8rem;
      letter-spacing: 1px;
      text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.1);
    }

    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 8px 18px rgba(0, 0, 0, 0.12);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      overflow: hidden;
      cursor: pointer;
      background-color: #fff;
    }

    .card:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 14px 30px rgba(0, 0, 0, 0.2);
    }

    .card img {
      border-radius: 20px 20px 0 0;
      height: 260px;
      object-fit: cover;
      transition: transform 0.3s ease;
      width: 100%;
    }

    .card:hover img {
      transform: scale(1.1);
    }

    .card-body {
      padding: 25px 20px;
    }

    .card-title {
      font-weight: 700;
      font-size: 1.5rem;
      margin-bottom: 12px;
      color: #ff6f00;
      letter-spacing: 0.03em;
    }

    .card p {
      font-size: 1.1rem;
      color: #555;
      margin-bottom: 18px;
    }

    .btn-success {
      background-color: #ff6f00;
      border: none;
      font-weight: 700;
      padding: 10px 22px;
      border-radius: 25px;
      transition: background-color 0.3s ease;
    }

    .btn-success:hover {
      background-color: #e65100;
    }

    /* --- Footer --- */
    footer {
      background-color: #ff7b00;
      color: white;
      text-align: center;
      padding: 25px 15px;
      font-weight: 600;
      font-size: 1.1rem;
      letter-spacing: 0.03em;
      margin-top: 60px;
      user-select: none;
    }

    /* --- Responsive --- */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2.5rem;
      }

      .hero p {
        font-size: 1.1rem;
      }

      .menu-preview h2 {
        font-size: 2rem;
      }
    }

    @media (max-width: 480px) {
      .hero h1 {
        font-size: 2rem;
      }

      .why-box {
        font-size: 1rem;
        padding: 18px;
      }

      .card img {
        height: 200px;
      }
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="index.html">FoodWheels India 🍔</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu" aria-controls="menu"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="menu">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link active" href="index.html">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="about.html">About</a></li>
          <li class="nav-item"><a class="nav-link" href="menu.html">Menu</a></li>
          <li class="nav-item"><a class="nav-link" href="contact.html">Contact</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section with background slider -->
  <section class="hero">
    <!-- Background images -->
    <div class="hero-bg active"
      style="background-image: url('https://images.unsplash.com/photo-1504674900247-0877df9cc836?auto=format&fit=crop&w=1500&q=80');">
    </div>
    <div class="hero-bg"
      style="background-image: url('https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?auto=format&fit=crop&w=1500&q=80');">
    </div>
    <div class="hero-bg"
      style="background-image: url('https://images.unsplash.com/photo-1542831371-d531d36971e6?auto=format&fit=crop&w=1500&q=80');">
    </div>

    <div class="hero-content">
      <h1>Street Food on Wheels 🚚</h1>
      <p>Fresh • Hygienic • Fast Delivery <br /> Taste the joy of Indian street food!</p>
      <a href="menu.html" class="btn-order">Explore Our Menu 🍕</a>
    </div>
  </section>

  <!-- Why Choose Us -->
  <section class="why-section container">
    <h2>Why Choose FoodWheels?</h2>
    <div class="row g-4">
      <div class="col-md-3 why-box">🍽 Freshly Cooked</div>
      <div class="col-md-3 why-box">🚚 Fast Delivery</div>
      <div class="col-md-3 why-box">💰 Affordable Prices</div>
      <div class="col-md-3 why-box">🧼 Hygienic Food</div>
    </div>
  </section>

  <!-- Menu Preview -->
  <section class="menu-preview container">
    <h2>Our Best Special Items 😋</h2>
    <div class="row justify-content-center g-4">

      <!-- Burger -->
      <div class="col-md-4 col-sm-6">
        <div class="card shadow-sm">
          <img src="https://images.unsplash.com/photo-1550547660-d9450f859349?auto=format&fit=crop&w=800&q=80"
            alt="Burger" />
          <div class="card-body text-center">
            <h5 class="card-title">Cheese Burst Burger</h5>
            <p>Juicy, cheesy, and full of flavor!</p>
            <a href="menu.html" class="btn btn-success">Order Now</a>
          </div>
        </div>
      </div>

      <!-- Pizza -->
      <div class="col-md-4 col-sm-6">
        <div class="card shadow-sm">
          <img src="https://images.unsplash.com/photo-1594007654729-407eedc4be65?auto=format&fit=crop&w=800&q=80"
            alt="Pizza" />
          <div class="card-body text-center">
            <h5 class="card-title">Veg Loaded Pizza</h5>
            <p>Fresh veggies & cheese overload!</p>
            <a href="menu.html" class="btn btn-success">Order Now</a>
          </div>
        </div>
      </div>

      <!-- Add more items if you want -->

    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>💖 Thank you for choosing FoodWheels India! Enjoy your meal and visit again soon!</p>
    <p>© 2025 FoodWheels India | Designed by Manasa 🚚🍔</p>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Hero background slider script -->
  <script>
    const backgrounds = document.querySelectorAll(".hero-bg");
    let current = 0;

    function nextBackground() {
      backgrounds[current].classList.remove("active");
      current = (current + 1) % backgrounds.length;
      backgrounds[current].classList.add("active");
    }

    setInterval(nextBackground, 6000); // Change every 6 seconds
  </script>

</body>

</html>
