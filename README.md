# Internship Task 2 – FoodWheels

## Home Page Screenshot
![FoodWheels Home Page](./src/assets/homepage.png)

---

## Objective
Recreate **Internship Task-1** using a **strict mandatory tech stack** with a templating engine and build system.

---

## Tech Stack Used
- **Templating Engine:** Nunjucks  
- **Bundler / Build Tool:** Vite  

---

## Folder Structure
- `docs/`  
  Contains the compiled HTML output generated after build.

> The `docs/` folder is used instead of `dist/` to enable GitHub Pages live preview.
- `src/templates/`  
  Contains all Nunjucks template files (`.njk`).

- `src/assets/`  
  Contains CSS, JavaScript, images, and screenshots.

---


## How to Run

### Install Dependencies, Build Project, and Open Output

Run the following commands step-by-step in your terminal:

```bash
# Install all dependencies
npm install

# Build the project
npm run build

# After build, open the output file in your browser
# You can open it manually by navigating to:
# docs/index.html
# Or open it directly from terminal (macOS/Linux):
open docs/index.html
# Or Windows PowerShell:
start docs/index.html

---


###
Output:
The final HTML output matches the Internship Task-1 document exactly. It is generated by rendering Nunjucks templates using the configured Vite build system, preserving:
-Formatting
-Emojis
-Styling
-Clickable links

Live Preview
You can view the live project here:
👉 https://manasa272.github.io/foodwheels-task2/
