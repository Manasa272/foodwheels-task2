# Internship Task 2 – FoodWheels


![Home Page Screenshot](./src/assets/homepage.png)

## Objective
Recreate the Internship Task-1 document using a **strict mandatory tech stack** with a templating engine and build tool.  
The final output exactly matches Task-1 in terms of formatting, emojis, styling, and links.

---

## Tech Stack Used
- **Templating Engine:** Nunjucks  
- **Bundler / Build Tool:** Vite  

---

## Folder Structure
docs/
└── index.html → Compiled HTML output after build
src/
├── templates/ → Nunjucks template files (.njk)
├── assets/ → CSS, JavaScript, images (including screenshots)

> **Note:**  
> The build output is generated in the `docs/` folder (instead of `dist/`) to enable GitHub Pages live preview.

---

## How to Run the Project

### 1. Install dependencies
```bash
npm install
2. Build the project
npm run build

3. View the output

Open the file below in your browser:

docs/index.html

Output

The final compiled HTML output is generated by rendering Nunjucks templates using Vite.
It matches the original Internship Task-1 document exactly, including:

Layout and formatting:
Emojis
Styling
Clickable links

Live Preview
You can view the live preview of the project here:
👉 https://manasa272.github.io/foodwheels-task2/

Architecture:
This project follows a Jamstack-style architecture, using static site generation with Nunjucks templates and a modern build process powered by Vite.
