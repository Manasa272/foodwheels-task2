# Internship Task 2 – FoodWheels

## Home Page Screenshot
![FoodWheels Home Page](./src/assets/homepage.png)

---

## Objective
Recreate **Internship Task-1** using a **strict mandatory tech stack** with a templating engine and build system.

---

## Tech Stack Used
- **Templating Engine:** Nunjucks  
- **Bundler / Build Tool:** Vite  

---

## Folder Structure
- `src/templates/`  
  Contains all Nunjucks template files (`.njk`).

- `src/assets/`  
  Contains CSS, JavaScript, images, and screenshots.

- `docs/`  
  Contains the compiled HTML output generated after build.

> The `docs/` folder is used instead of `dist/` to enable GitHub Pages live preview.

---

## How to Run

### Install dependencies
```bash
npm install
## Build the Project

Build the project using the following command:

```bash
npm run build
Open the Output

After building the project, open the following file in your browser to view the final result:
docs/index.html
Output

The final HTML output matches the Internship Task-1 document exactly.
It is generated by rendering Nunjucks templates using the configured Vite build system, preserving:

Formatting

Emojis

Styling

Clickable links

Live Preview

Check the live preview of the project here:
👉 https://manasa272.github.io/foodwheels-task2/
